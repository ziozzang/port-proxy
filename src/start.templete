cpwd=`pwd`
tmp="/tmp"
PID_FILE="$pwd/port-proxy.pid"


if [ -e $PID_FILE ]
then
	echo "[port-proxy] Already started...."
	exit -1
fi

cd $pwd
$pwd/port-proxy.pl > $pwd/port-proxy.log &
cd $cpwd
monpid=$!
echo $monpid > $PID_FILE

echo "[port-proxy] Started... pid=$monpid"

